# pytest.ini
[pytest]
minversion = 6.0
addopts = -ra -q
testpaths = 
    fieldmate/tests
python_files = test_*.py
markers =
    integration: marks tests that require a DB or full environment
    export: marks tests for export-related logic
    smoke: basic tests that check for script execution
